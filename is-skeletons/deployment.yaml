apiVersion: v1
kind: ConfigMap
metadata:
  name: skeletons
  namespace: default
data:
  detector: |-
    {
      "broker_uri": "amqp://rabbitmq.default",
      "zipkin_host": "zipkin.default",
      "zipkin_port": 9411,
      "models_folder": "/",
      "resize": {
        "width": 720,
        "height": 544
      },
      "model": "MOBILENET_THIN",
      "resize_out_ratio": 4.0
    }
  grouper_0: |-
    {
      "broker_uri": "amqp://rabbitmq.default",
      "zipkin_host": "zipkin.default",
      "zipkin_port": 9411,
      "cameras_ids": [0, 1, 2, 3, 4],
      "referencial": 1000,
      "min_error": 50.0,
      "period_ms": 200
    }
  grouper_1: |-
    {
      "broker_uri": "amqp://rabbitmq.default",
      "zipkin_host": "zipkin.default",
      "zipkin_port": 9411,
      "cameras_ids": [0, 2, 4, 6],
      "referencial": 1000,
      "min_error": 50.0,
      "period_ms": 200
    }
  heatmap_1000: |-
    {
      "broker_uri": "amqp://10.10.2.15:30000",
      "zipkin_host": "10.10.2.15",
      "zipkin_port": 30200,
      "topic_id": 1000,
      "limits": {
        "xmin": -2.5,
        "xmax": 7.5,
        "ymin": -2.5,
        "ymax": 12.5
      },
      "bins_step": 0.05,
      "output_scale": 5.0,
      "output_rotate": "ROTATE_90_CW",
      "referencial": {
        "id": 1000,
        "x": 0.0,
        "y": 0.0,
        "length": 50
      },
      "draw_grid": true,
      "flip_horizontal": true,
      "flip_vertical": false,
      "log_scale": true,
      "average_coordinates": false,
      "samples": 10000
    }

---

apiVersion: extensions/v1beta1 
kind: Deployment
metadata:
  name: "is-skeletons-detector"
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: detector
    spec:
      containers:
      - name: "0"
        image: viros/is-skeletons:1.1-detector
        command: ["python"]
        args: ["stream.py", "/opt/options.json"]
        imagePullPolicy: Always
        resources:
          limits: 
            cpu: "2"
            memory: 2048Mi
            nvidia.com/gpu: 1
        volumeMounts:
          - name: options
            mountPath: /opt
      - name: "1"
        image: viros/is-skeletons:1.1-detector
        command: ["python"]
        args: ["stream.py", "/opt/options.json"]
        imagePullPolicy: Always
        resources:
          limits: 
            cpu: "2"
            memory: 2048Mi
        volumeMounts:
          - name: options
            mountPath: /opt
      - name: "2"
        image: viros/is-skeletons:1.1-detector
        command: ["python"]
        args: ["stream.py", "/opt/options.json"]
        imagePullPolicy: Always
        resources:
          limits: 
            cpu: "2"
            memory: 2048Mi
        volumeMounts:
          - name: options
            mountPath: /opt
      - name: "3"
        image: viros/is-skeletons:1.1-detector
        command: ["python"]
        args: ["stream.py", "/opt/options.json"]
        imagePullPolicy: Always
        resources:
          limits: 
            cpu: "2"
            memory: 2048Mi
        volumeMounts:
          - name: options
            mountPath: /opt
      volumes:
      - name: options
        configMap:
          name: skeletons
          items:
          - key: detector
            path: options.json

---

apiVersion: extensions/v1beta1 
kind: Deployment
metadata:
  name: "is-skeletons-grouper-1000"
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: grouper
    spec:
      containers:
      - name: "0"
        image: viros/is-skeletons-grouper:2.2
        command: ["/service.bin"]
        args: ["/opt/options_0.json"]
        imagePullPolicy: Always
        resources:
          limits: 
            cpu: "1"
            memory: 128Mi
        volumeMounts:
          - name: options
            mountPath: /opt
      - name: "1"
        image: viros/is-skeletons-grouper:2.2
        command: ["/service.bin"]
        args: ["/opt/options_1.json"]
        imagePullPolicy: Always
        resources:
          limits: 
            cpu: "1"
            memory: 128Mi
        volumeMounts:
          - name: options
            mountPath: /opt
      volumes:
      - name: options
        configMap:
          name: skeletons
          items:
          - key: grouper_0
            path: options_0.json
          - key: grouper_1
            path: options_1.json

---

apiVersion: extensions/v1beta1 
kind: Deployment
metadata:
  name: "is-skeletons-heatmap-1000"
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: heatmap
    spec:
      containers:
      - name: "0"
        image: viros/is-skeletons-heatmap:1.2
        command: ["python"]
        args: ["service.py", "/opt/options.json"]
        imagePullPolicy: Always
        resources:
          limits: 
            cpu: "1"
            memory: 256Mi
        volumeMounts:
          - name: options
            mountPath: /opt
      volumes:
      - name: options
        configMap:
          name: skeletons
          items:
          - key: heatmap_1000
            path: options.json
